{
  "name": "filethetic-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Filethetic - Generate synthetic data with verifiable LLMs on EVM blockchains using Filecoin & IPFS",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean",
    "setup": "npm install && npx hardhat compile",
    "build": "npm run clean && npm run compile",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:doma-testnet": "hardhat run scripts/deploy.js --network domaTestnet",
    "deploy:doma-mainnet": "hardhat run scripts/deploy.js --network domaMainnet",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:base-sepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:base": "hardhat run scripts/deploy.js --network base",
    "deploy:fractional:doma-testnet": "hardhat run scripts/deploy-fractional.js --network domaTestnet",
    "deploy:fractional:sepolia": "hardhat run scripts/deploy-fractional.js --network sepolia",
    "verify": "hardhat verify",
    "extract-abi": "node scripts/extract-abi.js",
    "node": "hardhat node",
    "console": "hardhat console"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "filecoin",
    "ipfs",
    "synthetic-data",
    "llm",
    "verifiable"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@typechain/ethers-v6": "^0.5.0",
    "@typechain/hardhat": "^9.0.0",
    "chai": "^4.2.0",
    "dotenv": "^16.0.0",
    "ethers": "^6.4.0",
    "hardhat": "^2.26.3",
    "hardhat-gas-reporter": "^1.0.8",
    "solidity-coverage": "^0.8.0",
    "typechain": "^8.3.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6"
  }
}
